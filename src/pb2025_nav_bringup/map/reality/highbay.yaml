image: highbay.pgm
mode: trinary
resolution: 0.05
origin: [-12.8, -3.76, 0]
negate: 0
occupied_thresh: 0.65
free_thresh: 0.196

# The highbay localization map uses the surveyed floor as map-frame z=0.
# Add bounded slope/platform patches when this map is extended beyond the flat hall.
ground_elevation:
  default_height: 0.0
  patches: []
